function addRemove(input) {
    let numbers = [];
    for(let i = 0; i < input.length; i++) {
        if(input[i] == 'add') {

        } else if (input[i] == 'remove') {
            
        }
    }
}