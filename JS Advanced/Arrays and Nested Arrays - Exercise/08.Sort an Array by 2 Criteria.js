function sort(input) {
    let result = input.sort((a,b) =>b.length - a.length);
  
}

sort(['alpha', 
'beta', 
'gamma']
);

sort(['Isacc', 
'Theodor', 
'Jack', 
'Harrison', 
'George']
);

sort(['test', 
'Deny', 
'omen', 
'Default']
);